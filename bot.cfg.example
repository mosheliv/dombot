
-- Change these:
jid, password = "XXX@YYY.com", "secretpassword";
passcode="x"; --- the bot will not answer unless commands are prefixed by this pass code, for example "x list"

command_modules = {"list", "flick"}; --- list of modules to load. each module handle one or more commands

server_url = "http://localhost:8080";

